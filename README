This repository contains the code for Veer's and Jeff's MIT 6.858 final project.

Dependency list:
  capstone
  pyelftools
  z3 (with z3 python bindings)
  pwntools (for the example bof)

Setup:

capstone
  - sudo pip install capstone
pyelftools
  - It can be installed from: https://github.com/eliben/pyelftools
  - the pyelftools package in pip repos is old
z3
  - It can be installed from: https://github.com/Z3Prover/z3
pwntools
  - Pwntools is a set of python libraries that ease the process of exploit development (process wrappers, gdb support, etc).
  - It's only needed to run the example scripts.  It's not used in the actual rop_compiler library.
  - It can be installed from: https://github.com/Gallopsled/pwntools
